Grade: 9 / 10 

Double check your functions! You pass in a parameter that you don't actually use in the body of your 'get_title' and 'get_votes' functions. It happens to work out because the variable name that you use inside your function happens to be the one that you ultimately pass to it when you call your function further down in your code. 